# Used for production

services:
  fe-prod:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '10000:3000'
    environment:
      - NEXT_PUBLIC_API_URL='https://fleet-docs.cybermicroservices.com/api'