# Used for production

services:
  fe-prod:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '9000:3000'
    env:
      - AUTH_API_BASE_URL='https://fleet-docs.cybermicroservices.com/api'